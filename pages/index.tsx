import * as React from 'react';
import { NextPage } from 'next';

import Container from '@material-ui/core/Container';

const Home: NextPage = () => {
  return <Container />;
};

export default Home;
